doctype html
html(lang="es")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Eventify | Acceso
    style.
      @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&display=swap');
      :root { color-scheme: dark; }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: 'Space Grotesk', system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, sans-serif;
        min-height: 100vh;
        background: radial-gradient(circle at 15% 20%, rgba(244,114,182,0.25), transparent 45%), radial-gradient(circle at 80% 0%, rgba(56,189,248,0.25), transparent 40%), #03010f;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 48px 16px;
        color: #f8fafc;
      }
      .auth-shell {
        width: min(420px, 100%);
        background: rgba(8, 12, 40, 0.95);
        border-radius: 32px;
        padding: 40px 36px 32px;
        box-shadow: 0 25px 80px rgba(2, 6, 23, 0.65);
        border: 1px solid rgba(255,255,255,0.08);
        backdrop-filter: blur(20px);
      }
      .logo {
        display: flex;
        flex-direction: column;
        gap: 4px;
        margin-bottom: 28px;
      }
      .logo span {
        font-size: 1.6rem;
        font-weight: 700;
        letter-spacing: -0.01em;
      }
      .logo small {
        color: rgba(248,250,252,0.7);
      }
      .row { display: flex; flex-direction: column; gap: 14px; }
      label { font-weight: 600; margin-bottom: 6px; display: inline-block; }
      input {
        width: 100%;
        padding: 14px;
        border-radius: 14px;
        border: 1px solid rgba(255,255,255,0.15);
        font-size: 1rem;
        background: rgba(255,255,255,0.04);
        color: #f8fafc;
      }
      input::placeholder { color: rgba(248,250,252,0.55); }
      button, .btn {
        width: 100%;
        padding: 14px 16px;
        border-radius: 14px;
        border: 0;
        font-weight: 600;
        cursor: pointer;
        background: linear-gradient(120deg, #38bdf8, #f472b6);
        color: #05021a;
      }
      .link {
        margin-top: 18px;
        text-align: center;
        font-size: 0.95rem;
      }
      .link a { color: #facc15; text-decoration: none; font-weight: 600; }
      .alert {
        background: rgba(248,113,113,0.18);
        border-radius: 14px;
        padding: 12px;
        color: #fecaca;
        font-size: 0.95rem;
        margin-bottom: 18px;
        border: 1px solid rgba(248,113,113,0.35);
      }
      .back-home {
        margin-top: 24px;
        text-align: center;
        font-size: 0.9rem;
        color: rgba(248,250,252,0.7);
      }
      .back-home a { color: inherit; font-weight: 500; }
  body
    .auth-shell
      .logo
        span Eventify Task Manager
        small Gestioná tareas, clientes y proyectos con estilo
      block content
      .back-home
        span ¿Querés ver la demo? 
        a(href='/') Volver al inicio

