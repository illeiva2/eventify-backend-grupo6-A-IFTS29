doctype html
html(lang="es")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Eventify | Acceso
    style.
      :root { color-scheme: light; }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, sans-serif;
        min-height: 100vh;
        background: radial-gradient(circle at top, #93c5fd 0%, #e0f2fe 45%, #f8fafc 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 32px 16px;
        color: #0f172a;
      }
      .auth-shell {
        width: min(420px, 100%);
        background: #fff;
        border-radius: 28px;
        padding: 36px 32px 32px;
        box-shadow: 0 25px 80px rgba(15, 23, 42, 0.18);
        border: 1px solid #eff4ff;
      }
      .logo {
        display: flex;
        flex-direction: column;
        gap: 4px;
        margin-bottom: 28px;
      }
      .logo span {
        font-size: 1.5rem;
        font-weight: 700;
        letter-spacing: -0.01em;
      }
      .logo small {
        color: #475569;
      }
      .card {
        border-radius: 18px;
        padding: 0;
        background: transparent;
        box-shadow: none;
        margin: 0;
      }
      .row { display: flex; flex-direction: column; gap: 12px; }
      label { font-weight: 600; margin-bottom: 6px; display: inline-block; }
      input {
        width: 100%;
        padding: 12px 14px;
        border-radius: 12px;
        border: 1px solid #d0d7e6;
        font-size: 1rem;
      }
      button, .btn {
        width: 100%;
        padding: 12px 16px;
        border-radius: 12px;
        border: 0;
        font-weight: 600;
        cursor: pointer;
      }
      button { background: #0f172a; color: #fff; }
      .link {
        margin-top: 16px;
        text-align: center;
        font-size: 0.95rem;
      }
      .link a { color: #1d4ed8; text-decoration: none; font-weight: 600; }
      .alert {
        background: #fee2e2;
        border-radius: 12px;
        padding: 12px;
        color: #b91c1c;
        font-size: 0.95rem;
        margin-bottom: 16px;
      }
      .back-home {
        margin-top: 20px;
        text-align: center;
        font-size: 0.9rem;
        color: #475569;
      }
      .back-home a { color: inherit; font-weight: 500; }
  body
    .auth-shell
      .logo
        span Eventify Task Manager
        small Gestioná tareas, clientes y proyectos con estilo
      block content
      .back-home
        span ¿Querés ver la demo? 
        a(href='/') Volver al inicio

