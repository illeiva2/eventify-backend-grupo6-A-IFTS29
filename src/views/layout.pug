doctype html
html(lang="es")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Eventify Task Manager
    style.
      :root {
        color-scheme: light;
      }
      * { box-sizing: border-box; }
      body {
        font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, sans-serif;
        margin: 0;
        padding: 0;
        background: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%);
        color: #0f172a;
      }
      header {
        background: linear-gradient(120deg, #0f172a 0%, #1d4ed8 80%);
        color: white;
        padding: 24px 16px 32px;
        box-shadow: 0 10px 25px rgba(15, 23, 42, 0.15);
      }
      .header-content {
        max-width: 1080px;
        margin: 0 auto;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
      }
      .brand h1 {
        margin: 0;
        font-size: 1.6rem;
        letter-spacing: -0.02em;
      }
      .brand p {
        margin: 4px 0 0;
        opacity: 0.8;
        font-size: 0.95rem;
      }
      .primary-nav {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        align-items: center;
      }
      .primary-nav a {
        color: rgba(255,255,255,0.85);
        font-weight: 500;
      }
      .primary-nav a:hover { color: #fff; }
      .session-nav {
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 0.95rem;
      }
      .session-nav form {
        margin: 0;
      }
      .session-nav button {
        background: rgba(255,255,255,0.15);
        border: 1px solid rgba(255,255,255,0.4);
      }
      a { text-decoration: none; }
      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 10px 18px;
        border-radius: 999px;
        font-weight: 600;
        transition: all 0.2s ease;
      }
      .btn-primary {
        background: #facc15;
        color: #0f172a;
        box-shadow: 0 10px 20px rgba(250, 204, 21, 0.25);
      }
      .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 15px 30px rgba(250,204,21,0.35); }
      .btn-secondary {
        border: 1px solid rgba(255,255,255,0.6);
        color: #fff;
        background: transparent;
      }
      .btn-secondary:hover { border-color: #fff; color: #f8fafc; }
      main {
        padding: 24px;
        max-width: 1080px;
        margin: -10px auto 32px;
      }
      .card {
        border-radius: 20px;
        padding: 24px;
        margin-bottom: 20px;
        background: #fff;
        box-shadow: 0 10px 40px rgba(15, 23, 42, 0.08);
      }
      .card h2 { margin-top: 0; }
      .row {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
      }
      .row > * {
        flex: 1;
        min-width: 260px;
      }
      input, select, textarea {
        width: 100%;
        padding: 12px;
        border: 1px solid #d0d7e6;
        border-radius: 12px;
        font-size: 1rem;
      }
      button {
        padding: 12px 18px;
        border-radius: 12px;
        border: 0;
        background: #111827;
        color: white;
        cursor: pointer;
        font-weight: 600;
      }
      button:hover { opacity: 0.92; }
      table {
        width: 100%;
        border-collapse: collapse;
        overflow: hidden;
        border-radius: 16px;
      }
      th, td {
        border-bottom: 1px solid #edf0f7;
        padding: 12px;
        text-align: left;
      }
      th {
        background: #f8fafc;
        font-weight: 600;
      }
      @media (max-width: 720px) {
        .header-content {
          flex-direction: column;
          align-items: flex-start;
        }
        .session-nav {
          width: 100%;
          justify-content: space-between;
          flex-wrap: wrap;
          gap: 8px;
        }
        .session-nav button {
          flex: 1;
        }
      }
  body
    header
      .header-content
        .brand
          h1 Eventify Task Manager
          p Gestioná tareas, clientes y proyectos con estilo
        nav.primary-nav
          a(href='/') Inicio
          a(href='/clients') Clientes
          a(href='/tasks') Tareas
          a(href='/clients/new') Alta Cliente
          a(href='/products') Productos
          a(href='/projects') Proyectos
          a(href='/departments') Áreas
        if currentUser
          .session-nav
            span Hola, #{(currentUser && currentUser.name) || (currentUser && currentUser.email) || 'Usuario'}
            form(method='POST', action='/auth/logout')
              button(type='submit') Cerrar sesión
    main
      block content
