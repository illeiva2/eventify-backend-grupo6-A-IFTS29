doctype html
html(lang="es")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Eventify Task Manager
    style.
      @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap');
      :root {
        color-scheme: dark;
        --gradient-bg: radial-gradient(circle at 10% 20%, rgba(255,138,255,0.12), transparent 45%), radial-gradient(circle at 80% 0%, rgba(59,130,246,0.12), transparent 55%), #05021a;
        --card-bg: rgba(8, 12, 40, 0.9);
        --border-color: rgba(255,255,255,0.08);
        --accent: #f472b6;
        --accent-2: #38bdf8;
        --accent-3: #facc15;
      }
      * { box-sizing: border-box; }
      body {
        font-family: 'Space Grotesk', system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, sans-serif;
        margin: 0;
        min-height: 100vh;
        background: var(--gradient-bg);
        color: #f8fafc;
        position: relative;
      }
      body::before {
        content: '';
        position: fixed;
        inset: 0;
        background: radial-gradient(circle at 20% 20%, rgba(244,114,182,0.18), transparent 40%), radial-gradient(circle at 80% 10%, rgba(56,189,248,0.15), transparent 35%);
        pointer-events: none;
        z-index: -1;
      }
      header {
        background: linear-gradient(120deg, rgba(5,2,26,0.95) 0%, rgba(31,41,101,0.95) 60%, rgba(67,99,239,0.95) 100%);
        color: white;
        padding: 28px 16px 48px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.4);
        border-bottom: 1px solid rgba(255,255,255,0.08);
        position: relative;
        overflow: hidden;
      }
      header::after {
        content: '';
        position: absolute;
        width: 320px;
        height: 320px;
        background: radial-gradient(circle, rgba(248,250,252,0.18) 0%, transparent 70%);
        top: -140px;
        right: -110px;
        filter: blur(4px);
        pointer-events: none;
      }
      .header-content {
        max-width: 1180px;
        margin: 0 auto;
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        justify-content: space-between;
        gap: 16px;
        position: relative;
        z-index: 2;
      }
      .header-left {
        display: flex;
        align-items: center;
        gap: 24px;
        flex: 1;
      }
      .header-right {
        display: flex;
        align-items: center;
        gap: 16px;
      }
      .brand h1 {
        margin: 0;
        font-size: 1.85rem;
        letter-spacing: -0.02em;
      }
      .brand p {
        margin: 6px 0 0;
        opacity: 0.85;
        font-size: 1rem;
      }
      .primary-nav, .session-nav {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        align-items: center;
      }
      .primary-nav a {
        color: rgba(255,255,255,0.82);
        font-weight: 500;
        padding: 8px 12px;
        border-radius: 999px;
        transition: background 0.2s ease, color 0.2s ease;
      }
      .primary-nav a:hover { background: rgba(255,255,255,0.1); color: #fff; }
      .session-nav span {
        font-size: 0.95rem;
        color: rgba(248,250,252,0.8);
      }
      .session-nav form { margin: 0; }
      .session-nav button {
        background: rgba(248,250,252,0.1);
        border: 1px solid rgba(255,255,255,0.35);
        padding: 10px 20px;
        border-radius: 999px;
        color: #fff;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s ease;
      }
      .session-nav button:hover { background: rgba(248,250,252,0.2); }
      a { text-decoration: none; }
      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 10px 18px;
        border-radius: 14px;
        font-weight: 600;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }
      .btn-primary {
        background: linear-gradient(120deg, #f472b6, #facc15);
        color: #0f172a;
        box-shadow: 0 10px 20px rgba(244,114,182,0.35);
      }
      .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 20px 35px rgba(244,114,182,0.45); }
      .btn-secondary {
        border: 1px solid rgba(255,255,255,0.4);
        color: #fff;
        background: transparent;
      }
      .btn-secondary:hover { border-color: #fff; color: #f8fafc; transform: translateY(-2px); }
      main {
        padding: 32px;
        max-width: 1180px;
        margin: -20px auto 40px;
      }
      .card {
        border-radius: 24px;
        padding: 28px;
        margin-bottom: 24px;
        background: var(--card-bg);
        border: 1px solid var(--border-color);
        box-shadow: 0 30px 60px rgba(5, 2, 26, 0.45);
      }
      .card h2 { margin-top: 0; color: #f8fafc; }
      .card h3 { margin-top: 0; color: var(--accent-2); }
      .row {
        display: flex;
        gap: 18px;
        flex-wrap: wrap;
      }
      .row > * {
        flex: 1;
        min-width: 260px;
      }
      .pill {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 4px 12px;
        border-radius: 999px;
        background: rgba(255,255,255,0.08);
        color: #f8fafc;
        font-size: 0.85rem;
      }
      input, select, textarea {
        width: 100%;
        padding: 12px;
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 14px;
        font-size: 1rem;
        background: rgba(255,255,255,0.04);
        color: #f8fafc;
      }
      input::placeholder, textarea::placeholder { color: rgba(248,250,252,0.5); }
      button {
        padding: 12px 18px;
        border-radius: 14px;
        border: 0;
        background: linear-gradient(120deg, #38bdf8, #818cf8);
        color: white;
        cursor: pointer;
        font-weight: 600;
      }
      button:hover { opacity: 0.95; }
      table {
        width: 100%;
        border-collapse: collapse;
        overflow: hidden;
        border-radius: 20px;
        background: rgba(15,23,42,0.6);
      }
      th, td {
        border-bottom: 1px solid rgba(255,255,255,0.08);
        padding: 14px;
        text-align: left;
      }
      th {
        background: rgba(255,255,255,0.05);
        font-weight: 600;
      }
      @media (max-width: 720px) {
        .header-content {
          flex-direction: column;
          align-items: flex-start;
        }
        .header-left {
          width: 100%;
          flex-direction: column;
          align-items: flex-start;
          gap: 16px;
        }
        .primary-nav {
          width: 100%;
          flex-wrap: wrap;
        }
        .header-right {
          width: 100%;
          justify-content: flex-end;
        }
        .session-nav {
          width: 100%;
          justify-content: space-between;
          flex-wrap: wrap;
          gap: 8px;
        }
        .session-nav button {
          flex: 1;
        }
        main {
          padding: 16px;
          margin-top: 0;
        }
      }
  body
    header
      .header-content
        .header-left
          .brand
            h1 Eventify Task Manager
            p Gestioná tareas, clientes y proyectos con estilo
          nav.primary-nav
            a(href='/') Inicio
            a(href='/clients') Clientes
            a(href='/tasks') Tareas
            a(href='/clients/new') Alta Cliente
            a(href='/products') Productos
            a(href='/projects') Proyectos
            a(href='/departments') Áreas
        .header-right
          if currentUser
            .session-nav
              span Hola, #{(currentUser && currentUser.name) || (currentUser && currentUser.email) || 'Usuario'}
              form(method='POST', action='/auth/logout')
                button(type='submit') Cerrar sesión
    main
      block content
